sudo: required 
language: node_js
node_js:
    - "stable"
    
services: 
    - docker
before_install: 
    - docker build -t GELIFENbaokou/sample-app -f Dockerfile.dev .
script:     
    - docker run -e CI=true GELIFENbaokou/sample-app npm run test -- --coverage --watchAll=false
